NAME=routes
SUMMARY=Periodic TARPNstat link-quality broadcast (send-routes-via-cq)
VERSION=1
REQUIRES=core
UNITS=tarpn-routes.service tarpn-routes.timer
# Only the timer is enabled; it pulls in the service on schedule.
ENABLE=tarpn-routes.timer
REQUIRED=false
BINARY=send-routes-via-cq
