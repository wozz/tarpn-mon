# Module manifest. See ../../docs/DESIGN.md for the full contract.
NAME=core
SUMMARY=Base layout, tarpn.conf and the tarpn.target service group
VERSION=1
REQUIRES=
UNITS=tarpn.target
ENABLE=tarpn.target
# core is mandatory: every other module depends on the layout it creates.
REQUIRED=true
